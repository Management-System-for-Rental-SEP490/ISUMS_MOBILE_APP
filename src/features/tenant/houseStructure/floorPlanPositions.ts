/**
 * Tỉ lệ khung ảnh mặt bằng gốc (945×831) — chỉ còn dùng cho `HouseIcon` giữ đúng tỉ lệ.
 * Bố cục khu vực chức năng trên màn chi tiết nhà dùng chip trong `FloorPlanSvg`, không đọc `position` từ BE.
 */
export const FLOOR_PLAN_IMAGE_ASPECT = 945 / 831;
