/**
 * Liên kết pháp lý / hỗ trợ ở footer Home (tenant) — đồng bộ ý với Keycloak `isums-mobile-tenant-theme`.
 * Điền URL HTTPS khi deploy; để trống thì chỉ hiển thị chữ (không mở trình duyệt).
 */
export const tenantFooterLinks = {
  privacyPolicy: "",
  termsOfUse: "",
  support: "",
};
